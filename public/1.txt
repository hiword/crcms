图集上传更改完成，未测试

tags用户添加，status状态，考虑在tagstoreservie增加一个接口



一个表（可以算成表，如果是多个复合表，以主表为主）
有4个接口，分别对应 		增，删，改，查，不同的增，删，改，查，需要多次在接口中增加相应 函数

查，函数相对复杂，可能包含，逻辑处理，缓存处理等等 ，都以 如：xxxService 服务中




controller可以根据情况调用多个  服务，但不调用Model


服务内部只调其内部表(model，也可调用其它model[因逻辑判断需要])，不可调用服务

