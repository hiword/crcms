控制器 负责 request  负责各个类的中转  接收和转发数据

service 是逻辑处理，模型数据获取，并处理  ,也包括判断缓存什么东西的

service 里面使用的数据，除了model,其它传部使用 参数传递

service 在数据添加，编辑的时候，负责处理添加其它内置数据和对传入的数据进行格式化  如ip 127.0.0.1 转换成数字

field数据  负责  字段 添加，编辑，显示时候的格式输出 或输入，和对于字段输入的验证
和对于不同路由场景的判断输入，输出  可能需要包含request对象

model 只负责数据的添加和获取  读取和存储数据